{
  "name": "liquid-downloader-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker backend for Liquid Downloader",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
