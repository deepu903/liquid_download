{
  "name": "social-downloader-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm run start:backend",
    "start:frontend": "npm start --prefix frontend",
    "start:backend": "npm start --prefix backend",
    "dev": "npx concurrently \"npm run start:frontend\" \"npm run start:backend\"",
    "build": "npm run build --prefix frontend",
    "setup": "npm install --prefix frontend && npm install --prefix backend",
    "setup:all": "npm install && npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "@angular/cli": "^21.2.3",
    "concurrently": "^9.2.1"
  }
}
